@using BizData.Entities
@model IEnumerable<UnidadOrganizativa>

<style>
    #myTable tr.header, #myTable tr:hover {
        /* Add a grey background color to the table header and on hover */
        background-color: #f7cfcf;
    }
</style>

<section>
    <input type="text" id="myInput" onkeyup="myFunction()" placeholder="Search for names..">
</section>
<section>
    <form method="post">
        <table id="myTable">
            <tr>
                <th>
                    Nombre
                </th>
                <th>
                    Provincia
                </th>
                <th>
                </th>
            </tr>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @item.Nombre
                    </td>
                    <td>
                        @item.Provincia.Nombre
                    </td>
                   
                    <td>
                        <button class="button" type="submit" value="Edit/@item.UnidadOrganizativaID" name="button">   <span>Editar</span> </button>
                    </td>
                </tr>
            }
        </table>
        <button class="button" type="submit" value="Add" name="button">   <span>Añadir</span> </button>
    </form>
</section>